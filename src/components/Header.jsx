export default function Header() {
    return (
        <header>
            <nav className="navbar">
                <span>The one-stop tool to convert images into Canvas.js code</span>
            </nav>
        </header>
    )
}